// A small rotating palette of dot colors for lists that want a bit of color
// variety without inventing a new meaning-bearing system (unlike the
// project category hues, these don't map to anything — they just cycle).
export const accentDots = ["bg-cat-algorithms", "bg-cat-ml", "bg-cat-web", "bg-cat-embedded"];
